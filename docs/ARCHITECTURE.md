# HydroGuard Architecture

Arduino/ESP32 → IoT communication → Spring Boot backend → Supabase PostgreSQL → React frontend.

The backend is the API/application layer between hardware, database and browser.
